
let array =[22,56,89,12,90,44,41,31,23];

console.log(array);

function dob(ele){
    return e*e
}

// map is used to operate in each elemnt  function 
array.map(e=>e*e).forEach(e=>console.log(e));

console.log()
// filter is used to filter out the element (conditon)

array.filter(e=>e%2==0).forEach(e=>console.log(e));

// reduce is used to itertae each function of array and reduce it takes two elements of array
console.log();


c=array.reduce((a,b)=>a+b);
console.log(c);

let arr = [3,8,21,4,2,9,17];
console.log(arr);

brr = arr.sort((a,b)=>a-b);
console.log(brr)